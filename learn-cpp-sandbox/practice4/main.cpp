#include "lesson_13_pt_2.h"
#include <iostream>


int main()
{

    // AdRevenue::something();
    // ViewVsOwn::something();
    // MixingPointers::something();

    // ClassTempPrac::Pair<int, double> p1 { 10, 15.6 };
    // ClassTempPrac::something(p1);

    // MoreTemplate::Pair p1 { 3.4, 5.67 };
    // MoreTemplate::Point p2 { 56, 45 };
    // MoreTemplate::something(p1);
    // MoreTemplate::something(p2);

    // Pairs::something();
    MonstersQuiz::something();

    return 0;
}